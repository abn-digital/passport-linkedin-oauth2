<% if (!user) { %>
	<h2>Welcome! Please log in.</h2>
	<a href="/auth/linkedin">Login with Linkedin</a>
<% } else { %>
	<h2>Hello, <%= user.displayName %>. Our robots are already working for you!</h2>
<% } %>
